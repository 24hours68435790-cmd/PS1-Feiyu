# PS1 companion requirements.
#
# The course template pinned numpy/scipy/matplotlib/nashpy for the class LLM
# example (a logit cognitive hierarchy demo). This project replaces that example
# with the strategic-disclosure model, which needs only three packages; pandas
# is added for the sweep tables. scipy and nashpy are no longer imported.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
